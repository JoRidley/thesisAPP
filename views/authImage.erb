

<div class='jumbotron text-center'>
	<div class='alert'></div>
	<div class='selected'></div>
	<div data-id="<%= @user.id %>" data-version="<%= @version %>" id="user" ></div>
	<div><span id="progress">0</span>/5 Selected</div>
  <div class='remove-last empty'>remove last</div>
  <div class='signin-container'>
  	<% @image_urls.each do |image| %>
  		<img class='login' src='/<%= image.url %>' id='<%= image.id %>' />
  	<% end %>
  </div>
</div>

<style>

  body {
    background-color: rgba(0,0,0, 0.4);
  }
</style>
